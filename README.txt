This is the new git repository for PHDL.

We want to migrate things over here from Subversion, but not just copy things
in-mass, as there is a lot of cruft that is in Subversion that we don't want in
git (e.g. compiled or generated code; 3rd-party or non-project code; outdated
onld history files, etc).
